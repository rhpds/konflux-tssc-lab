# Custom trusted task rules for lab pipeline bundle
# Our tasks come from a BUNDLE, not individual OCI images
# Try trusting the bundle itself
---
rule_data:
  trusted_task_rules:
    allow:
      lab-custom-bundle:
        # Trust the bundle - all tasks resolved from it
        - pattern: oci://quay.io/rhpds/pipeline-docker-build-lab
