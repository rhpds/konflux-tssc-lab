# Custom trusted task rules for lab pipeline bundle
# Trust all tasks from our custom bundle using wildcard pattern
---
rule_data:
  trusted_task_rules:
    allow:
      lab-custom-bundle:
        # Trust any digest from our bundle repository
        - pattern: oci://quay.io/rhpds/pipeline-docker-build-lab@sha256:.*
