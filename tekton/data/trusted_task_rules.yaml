# Custom trusted task rules for lab pipeline bundle
# Match the bundle URL with version tag
---
rule_data:
  trusted_task_rules:
    allow:
      lab-custom-bundle:
        # Trust all versions of our bundle
        - pattern: oci://quay.io/rhpds/pipeline-docker-build-lab:*
